{
  "config": {
    "step": {
      "user": {
        "title": "Location & radius",
        "description": "Configure where to search for aircraft. Defaults to your Home Assistant home location.",
        "data": {
          "latitude": "Latitude",
          "longitude": "Longitude",
          "radius_km": "Search radius (km)",
          "refresh_minutes": "Refresh interval (minutes)"
        }
      },
      "apis": {
        "title": "API keys (optional)",
        "description": "All fields are optional. OpenSky OAuth increases rate limits. AeroDataBox provides live departure/destination data. Cache dir must be the .cache path from closest-plane-app if you want to share manual overrides.",
        "data": {
          "opensky_client_id": "OpenSky client ID",
          "opensky_client_secret": "OpenSky client secret",
          "aerodatabox_api_key": "AeroDataBox RapidAPI key",
          "cache_dir": "Manual cache directory path"
        }
      }
    }
  }
}
